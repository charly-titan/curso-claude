/**\n * Validador de email.\n *\n * Versión inicial con regex naïve — pendiente de hardening.\n * Acepta formatos inválidos como `usuario@.com`.\n * Véase issue #1.\n */\n\n// Regex simple: cualquier cosa + @ + cualquier cosa + . + cualquier cosa.\n// Problemas conocidos: no ancla, permite punto al inicio del dominio,\n// permite TLD vacío, permite espacios, etc.\nconst EMAIL_REGEX = /.+@.+\\..+/;\n\nexport function isValidEmail(email) {\n  if (typeof email !== "string") return false;\n  return EMAIL_REGEX.test(email);\n}\n